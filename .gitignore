node_modules/
__pycache__/
.DS_Store
runs/
screenshots/
public-bot/
public-client/

server.json
.env

backups/
.local/
.obsidian/

# Bot folders (except template)
bots/*/
!bots/_template/

# Daemon type checker temp file
sdk/__exec_check.ts

# Upstream npm lockfile; this repo builds the engine with bun/bun.lock only.
# Deleted locally — ignore so vendor syncs don't silently reintroduce it.
server/engine/package-lock.json

# SDK is copied into Docker via Dockerfile; don't duplicate it under server/
server/sdk/
jobs/

# Local Windows runtime state
server/engine/*.sqlite-shm
server/engine/*.sqlite-wal
# Legacy cache path created by older lite-client sessions when HOME was unset.
server/webclient/undefined/
